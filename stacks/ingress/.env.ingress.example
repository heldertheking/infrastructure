STACK_NAME=ingress

PATH_APPDATA=/opt/appdata/ingress

TZ=Europe/Zurich
DOMAIN=example.com

# Cloudflare Tunnel token — generate at dash.cloudflare.com > Zero Trust > Networks > Tunnels
CF_TUNNEL_TOKEN=your-cloudflare-tunnel-token-here
