STACK_NAME=pelican

PATH_APPDATA=/opt/appdata/pelican

TZ=Europe/Zurich
DOMAIN=example.com

# MariaDB credentials — use strong, unique passwords
PELICAN_DB_ROOT_PASSWORD=change-me-root-password
PELICAN_DB_PASSWORD=change-me-pelican-password
