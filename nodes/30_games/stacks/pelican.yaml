services:
  panel:
    image: ghcr.io/pelican-dev/panel:latest
    container_name: pelican-panel
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    depends_on:
      - database
      - redis
    environment:
      - TZ=${TZ}
      - APP_URL=https://pelican.heldertheking.com
      - APP_ENV=production
      - APP_DEBUG=false
      - BEHIND_PROXY=true
    volumes:
      - ${PATH_APPDATA}/pelican/panel:/pelican-data
    networks:
      - pelican-net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.pelican-panel.rule=Host(`pelican.heldertheking.com`)"
      - "traefik.http.routers.pelican-panel.entrypoints=web"
      - "traefik.http.services.pelican-panel.loadbalancer.server.port=8080"

  database:
    image: mariadb:11
    container_name: pelican-db
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    environment:
      - TZ=${TZ}
      - MYSQL_ROOT_PASSWORD=${PELICAN_DB_ROOT_PASSWORD}
      - MYSQL_DATABASE=pelican
      - MYSQL_USER=pelican
      - MYSQL_PASSWORD=${PELICAN_DB_PASSWORD}
    volumes:
      - ${PATH_APPDATA}/pelican/db:/var/lib/mysql
    networks:
      - pelican-net

  redis:
    image: redis:7-alpine
    container_name: pelican-redis
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    environment:
      - TZ=${TZ}
    volumes:
      - ${PATH_APPDATA}/pelican/redis:/data
    networks:
      - pelican-net

  wings:
    image: ghcr.io/pelican-dev/wings:latest
    container_name: wings
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    environment:
      - TZ=${TZ}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /var/lib/docker/volumes:/var/lib/docker/volumes
      - /etc/pelican:/etc/pelican
      - ${PATH_STORAGE}/wings:/var/lib/pelican/volumes
      - /tmp/pelican:/tmp/pelican
    networks:
      - pelican-net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.wings.rule=Host(`wings.heldertheking.com`)"
      - "traefik.http.routers.wings.entrypoints=web"
      - "traefik.http.services.wings.loadbalancer.server.port=8080"

networks:
  pelican-net:
    name: pelican-net
    driver: bridge